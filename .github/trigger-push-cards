# Touch this file (any change) to (re)publish the workflow catalog cards:
# POSTs digests/workflows/recommended.json to the automation ingest endpoint.
# Fast path — no fetch/analyzer. Payload shape fix + republish: 2026-07-09
