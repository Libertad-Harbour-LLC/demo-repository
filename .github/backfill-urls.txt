# One GitHub repo URL per line; editing this file on main triggers the
# skill-backfill workflow (enrich + push to the catalog). Kept empty between
# runs so stray edits don't re-push. Fill in URLs and merge to run a backfill.
